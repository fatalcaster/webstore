export type TAddress = {
  firstName: string;
  lastName: string;
  address: string;
  apt?: string;
  city: string;
  country: string;
  state: string;
  postalCode: string;
};

// interface BillingAddress {}

// interface ShippingMethod {}
